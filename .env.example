# --- Supabase (backend) ---
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=   # server / edge functions ONLY. never ship to the app.

# --- Mobile app (public, safe to expose) ---
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_MAPBOX_TOKEN=

# --- Device integrity (anti-spoof for captures / anti-spam) ---
# Play Integrity (Android) + App Attest (iOS) are configured natively, not here,
# but any verification secrets the edge function needs go in server env.
