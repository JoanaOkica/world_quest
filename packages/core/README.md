# @terra/core

Pure, deterministic domain logic shared by the mobile app and the Supabase edge functions.
No React, no Node/Deno APIs, no side effects — just types, scoring, and validation.
Scoring must exist ONLY here so client and server can never disagree.
